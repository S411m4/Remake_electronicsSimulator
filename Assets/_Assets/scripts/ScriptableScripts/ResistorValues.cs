using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ResistorValues : MonoBehaviour
{//used as SO (couldn't use SO because every instance has its unique resistance value according to value chosen during runtime)

    public double ResistorValueOhm;

    public Material band1Color;
    public Material band2Color;
    public Material multiplierBandColor;


}
